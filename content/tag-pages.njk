---js
// <script>
const pagination = {
    data: "collections",
    size: 1,
    alias: "tag",
    filter: ["all", "posts", "postsByYear", "postsByYearMonth", "postsByYearMonthDay"],
    // filter: ["all", "posts"],
    // addAllPagesToCollections: true,
};

const eleventyExcludeFromCollections = true;

const eleventyComputed = {
    title: "Tagged '{{ tag }}'",
    permalink: function(data) {
        return `/tag/${this.slugify(data.tag)}/`;
    }
};
---
<h1>Tagged "{{ tag }}"</h1>

{% set postslist = collections[ tag ] %}
{% include "postslist.njk" %}

<p>See <a href="tag.njk">all tags</a>.</p>
